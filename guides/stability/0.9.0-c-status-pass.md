# `0.9.0-c` Status Pass

This note records the current status of the third `0.9.0` chunk:

* `0.9.0-c` release lock

The purpose is to separate:

* release-facing surfaces that already reflect the landed `0.9.0` decisions
* release-facing surfaces that still lag behind the chosen contract
* remaining cleanup that should happen before this line reads like a deliberate handoff to `1.0.0`

It is a repo-local planning artifact for the final pre-`1.0.0` stabilization line.
It is not intended as public package-facing guidance.

## Current Status Summary

Current high-level status:

* the runtime-facing `0.9.0-a` decisions are already reflected in the README and generated API surface
* the `0.9.0-b` boundary freeze is already reflected in the source, guides, and API wording
* the main `0.9.0-c` release-surface gaps are now landed:
  * `docs/releases/0.9.0.md` exists
  * `CHANGELOG.md` reflects the full `0.9.0` line
  * package metadata and the public guides index now thread the same release story

## `Must decide`

### 1. `0.9.0` Release-Note Presence

Status:

* `resolved`
* `landed`

Current state:

* the release history had no `docs/releases/0.9.0.md`
* prior release lines already use checked-in release notes as part of the package-facing history trail

Decision taken:

* add `docs/releases/0.9.0.md`
* use it as the package-facing summary of the full stabilization line rather than only the first chunk

Why this matters:

* `0.9.0` is not a tiny release-line patch; it is the last deliberate stabilization line before `1.0.0`
* if that line lacks a release note, the release history under-explains what the package has actually proven

### 2. Changelog Alignment With The Full `0.9.0` Line

Status:

* `resolved`
* `landed`

Previous state:

* `CHANGELOG.md` already covered the `0.9.0-a` contract cleanup
* it under-described the `0.9.0-b` boundary freeze and release-surface alignment work

Decision taken:

* keep the unreleased `0.9.0` changelog entry
* expand it so it reflects:
  * contract cleanup
  * boundary freeze
  * docs/website/release-surface alignment

Why this matters:

* `CHANGELOG.md` is one of the first public release surfaces evaluators and upgraders read
* it should describe the real purpose of the release line rather than just the earliest landed chunk

## `Should decide`

### 1. Final Release-Surface Consistency Sweep

Status:

* `resolved`
* `landed`

What already looks good:

* package-facing examples already use current primary names and current option posture
* README already reflects:
  * `StreamOrderBatch`
  * `compareClocks()` as deprecated compatibility surface
  * `compareDeterministically()` and `applyTieBreaker()` classification
  * the `strict` / `detectAnomalies` / `allowUnknownOrder` wording freeze

What still deserves a final pass:

* whether any later release-facing edits reopen older wording accidentally

### 2. `1.0.0` Transition Visibility

Status:

* `resolved`
* `landed`

What already exists:

* `README.md` calls out the planned `compareClocks()` removal at `1.0.0`
* `guides/upgrade-expectations.md` already explains the primary-name versus compatibility-alias posture
* `ROADMAP.md` already frames `0.9.0` as the pre-`1.0.0` handoff line

What remains:

* carry the same path forward when the actual `1.0.0` line is cut

## `Nice to have`

### 1. Cleaner Release-History Threading

Status:

* `open`

Optional improvement:

* make the `0.9.0` release note explicitly read as the bridge between:
  * the `0.8.0` adoption-maturity line
  * the eventual `1.0.0` stable public release

## What Can Be Ticked Right Now

The updated answer is:

* the runtime and boundary work are already locked
* the main release-surface alignment work is now landed
* the remaining work after this point belongs to the eventual `1.0.0` cut rather than to unresolved `0.9.0-c` ambiguity
